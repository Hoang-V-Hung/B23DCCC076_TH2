import RPS from "./rps";

const Index = () => {
  return (
    <div style={{ textAlign: "center", padding: "50px", backgroundColor: "#e6f7ff", minHeight: "100vh", display: "flex", justifyContent: "center", alignItems: "center" }}>
      <RPS />
    </div>
  );
};

export default Index;